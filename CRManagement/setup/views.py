from django.shortcuts import render

# Create your views here.
def setupDepartment(request):
    context={

    }

    return render(request,"setup/setupDepartment.html",context)


def setupSubsystem(request):
    context={

    }

    return render(request,"setup/setupSubsystem.html",context)